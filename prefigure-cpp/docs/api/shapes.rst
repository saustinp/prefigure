Boolean Geometry (Shapes)
=========================

The shape module provides boolean geometry operations on SVG paths using
the GEOS C API.  Shapes can be defined from any renderable element and
then combined using union, intersection, difference, symmetric-difference,
or convex-hull operations.

.. doxygenfile:: shape.hpp
   :project: prefigure
